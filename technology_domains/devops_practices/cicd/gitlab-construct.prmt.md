# Description: GitLab CI: Go project pipeline (build, test, static analysis, Docker image).
# Usage: Adapt for LLM input. Replace <placeholders> if present in specific constructs.
# ---

# GitLab CI Pipeline for Go Project Construct\nDescription: GitLab CI: Go project pipeline (build, test, static analysis, Docker image).\n<System-Instruction>:\nCreate a .gitlab-ci.yml file for a Go project that builds the binary, runs unit tests, performs static analysis (e.g., golangci-lint), and builds a Docker image. Include stages for dev and main branches, and demonstrate use of caching for Go modules and Docker layer caching.
